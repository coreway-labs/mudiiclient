apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'application'

sourceCompatibility = 11
targetCompatibility = 11

mainClassName = 'gui3.Launcher'

repositories {
    mavenCentral()
}

dependencies {
    // such ancient dependencies!
    testImplementation files('lib/test/junit.jar')
    testImplementation files('lib/test/jmock-1.1.0.jar')
}

jar {
  manifest {
    attributes(
      'Main-Class': 'gui3.Launcher'
    )
  }
}

